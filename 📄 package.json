{
  "name": "vite-test",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port $PORT",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port $PORT"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
